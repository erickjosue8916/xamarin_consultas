<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Tarea.FiltroPacientes"
             Title="Filtro">
    <ContentPage.Content>
        <StackLayout>
            <Grid MinimumHeightRequest="100">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Label Text="Fecha Inicio:" Grid.Column="0"/>
                <DatePicker Grid.Column="0" Margin="10,20,10,20"
                        x:Name="startDatePicker"
                        DateSelected="OnDateSelected"/>

                <Label Text="Fecha Final:" Grid.Column="1"/>
                <DatePicker Grid.Column="1" Margin="10,20,10,20"
                        x:Name="endDatePicker"
                        MinimumDate="{Binding Source={x:Reference startDatePicker},
                                          Path=Date}"
                        DateSelected="OnDateSelected"/>
            </Grid>

            <SearchBar Placeholder="Buscar pacientes..."
                       HorizontalTextAlignment="Center"
                       FontSize="Medium"
                       x:Name="searchPaciente"
                       TextChanged="searchPaciente_TextChanged"/>

            <StackLayout>
                <ListView x:Name="listView"
              Margin="20">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="100" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Label Grid.Column="0" Text="{Binding Fecha}" ></Label>
                                    <Label Grid.Column="1" Text="{Binding Observaciones}" ></Label>
                                    <Label Grid.Column="2" Text="{Binding NombrePaciente}" ></Label>
                                    <Label Grid.Column="3" Text="{Binding NombreTipoConsulta}" ></Label>
                                </Grid>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackLayout>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>